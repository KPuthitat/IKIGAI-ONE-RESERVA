Health: doctor-invite flow, AHC logo, disease bullets, unlock screen fix

Owner 2026-06-06 batch on the weight-control program:

1. Doctor-invite flow (replaces self-enrollment):
   - Remove the self-enroll banner from the staff home page.
   - Doctor sends an invitation via the new "ส่งคำเชิญ" button in the
     clinical dashboard; selects any eligible employee from a dropdown
     (/api/admin/mounjaro/eligible-employees).
   - Employee receives a LINE notification; sees invitation card on the
     health portal and confirms via one button.
   - Only confirmed invitations appear in the doctor's "ทำ Baseline" list.
   - Doctor's dashboard shows two separate panels: "รอยืนยัน" (invited
     but not confirmed) and "รอทำ Baseline" (confirmed, ready for intake).
   - Notifies doctor via LINE when employee confirms.
   - Schema: mounjaro_enrollments += invited_by_doctor_id, invited_at,
     employee_confirmed_at (PRAGMA-guarded ALTERs).
   - Gateway: inviteEmployee() / confirmMyInvitation() /
     listEligibleEmployees() / listPendingInvitations(); listPendingIntake
     now requires employee_confirmed_at IS NOT NULL.

2. AHC logo: replace the "M" placeholder badge in the clinical unlock
   screen and empty-state list with the AHC logo image.

3. Unlock screen text: "ข้อมูลผู้ป่วยเป็นข้อมูลอ่อนไหว —" removed;
   "กรุณายืนยันตัวตน" promoted to main heading; disclaimer text made
   smaller and more formal.

4. Disease names in bullet list: FlagList component changed from
   labels.join(", ") (one paragraph) to an unordered list with gold
   bullet dots — easier to read when multiple items are present.

5. Remaining "Mounjaro" text: PatientDetailClient metadata title updated
   to "โครงการควบคุมน้ำหนัก". "เพิ่มผู้ป่วยใหม่" replaced with invite
   flow. "ยังไม่มีพนักงานสมัคร" updated to match new invite model.

tsc + i18n + scan-build-risks + test-mounjaro (47) + test-rbac (23) green.
test-mounjaro updated: seeds enrollment via inviteEmployee + confirmMyInvitation.

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
